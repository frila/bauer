#ifndef BAUER_TYPES_HPP
#define BAUER_TYPES_HPP

#include <iostream>

typedef int bsocket_t;
typedef std::string bauer_ip;
typedef short bauer_port;
typedef std::streamoff bfile_size;
typedef std::streamoff bfile_chunks;
#endif